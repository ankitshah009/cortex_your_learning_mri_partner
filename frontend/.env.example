# Optional: real analyze endpoint (Butterbase serverless function).
# POST { problemId, reasoning } -> Diagnosis JSON (src/scenarios/types.ts).
# Leave unset to run fully on seeded demo content.
# VITE_ANALYZE_URL=https://your-butterbase-app.example.com/functions/analyze
